{
  "granted_by": "User via DENTIX_AUTHORIZATION.txt, DENTIX_NEXT_AUTHORIZATION_COMMIT_PUSH_DRAFT_PR.txt and DENTIX_NEXT_AUTHORIZATION_READY_FOR_REVIEW.txt supplied in this conversation",
  "granted_at": "2026-09-08T13:56:45.985Z",
  "expires_at": null,
  "repository": "optidigitalagent/dentix-booking-demo",
  "project_id": "dentix",
  "scope": "DENTIX ONLY; optidigitalagent/dentix-booking-demo; PR-00 / PR #1 on seo/dentix-pr00-bootstrap-t0 into main. Recheck base/head SHA, Draft status, mergeability and complete diff. Update only necessary .seo operational records to reflect supplied independent PR-00 acceptance and READY_FOR_REVIEW; update this authorization within the new grant; commit/push only bounded .seo metadata to the named branch; mark PR #1 ready for review; reverify GitHub and produce sanitized evidence. Existing branch/open_pr/draft_pr permissions remain purpose-scoped to PR-00 only. implement=false; merge=false; deploy=false; publish=false; release=false; release_close=false; sitemap_submit=false; indexing_request=false; external_action=false. No changes to public site/source, src/, public/, workflows, configs, dependencies, lockfiles, CRM, Railway, DNS, GSC, GBP, GA4/GTM, Bing or other external systems. No PR-01, messages, submissions, patient data, secrets or operator changes. Merge is prohibited: the current workflow deploys GitHub Pages on every push to main, so merge requires separate explicit merge and deploy authorization.",
  "permissions": {
    "audit": true,
    "branch": true,
    "edit": true,
    "implement": false,
    "commit": true,
    "push": true,
    "open_pr": true,
    "draft_pr": true,
    "ready_for_review": true,
    "merge": false,
    "deploy": false,
    "publish": false,
    "sitemap_submit": false,
    "external_action": false,
    "release": false,
    "release_close": false
  }
}
